language: go

os:
  - linux
  - windows
  - osx

go:
  - 1.12.1

script:
  - sed -i '' 's/trace = false/trace = true/g' notrace.go
  - go get -t ./...
  - go test -v -race -coverprofile=coverage.txt ./...

after_success:
  - bash <(curl -s https://codecov.io/bash)